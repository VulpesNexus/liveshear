Live Shear -- a parameter dictionary this version did not write
Run at 2026-09-14 01:54:14
Watchdog: 45 seconds per redraw

[PASS] no schema key at all means schema 1
       the first version wrote no schema key, and its documents must keep rendering; rendered 269.282 pt wide in 2,72 s, expected 269.282 pt
[PASS] a schema number from a later version still renders
       an unknown schema is not a reason to refuse to draw; the two angles mean what they have always meant; rendered 269.282 pt wide in 1,41 s, expected 269.282 pt
[PASS] a nonsense schema number still renders
       nothing about the number changes what the angles mean here; rendered 269.282 pt wide in 3,18 s, expected 269.282 pt
[PASS] no shear angle means no shear
       the default is zero, and zero is the identity; rendered 200 pt wide in 3,29 s, expected 200 pt
[PASS] no axis angle means the horizontal axis
       the default axis is zero, which is the shear the fixture already has; rendered 269.282 pt wide in 3,23 s, expected 269.282 pt
[PASS] a shear angle stored as text is refused
       reading a real out of a string entry fails, and a failed read leaves the default; rendered 200 pt wide in 3,32 s, expected 200 pt
[PASS] an axis angle stored as text is refused
       same, and the default axis is zero; rendered 269.282 pt wide in 3,28 s, expected 269.282 pt
[PASS] a shear angle stored as a flag is refused
       the type is wrong, so the default stands; rendered 200 pt wide in 3,31 s, expected 200 pt
[PASS] an empty parameter block is the identity
       every key absent is every default, and the defaults are zero; rendered 200 pt wide in 3,29 s, expected 200 pt

--- a key from a version that does not exist yet ---
       dialog driver: committed; edit effect said: EditEffectParameters on post-effect 0 returned 0
[PASS] a later version's extra keys survive the dialog writing the block back
       referencePoint kept, somethingElse kept after the plugin itself rewrote the block through the dialog
[PASS] the block records which version last wrote it
       shearSchema is 1 after this version wrote it, which is this version's number: the block says what it means rather than what it used to mean

11 passed, 0 failed
